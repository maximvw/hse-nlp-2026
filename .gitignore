# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables
.env

# Output files (processed videos, transcripts, FAISS indexes)
output/

# User data (passwords, chat history) — keep data/ dir itself tracked via .gitkeep
data/users.yaml
data/chats/
